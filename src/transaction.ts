export class Transaction {
  readonly id: string;
  readonly amount: number;
  readonly description: string;
  readonly ccLastFour: string;
  readonly ccExpiry: string;
  readonly ccToken: string;
  readonly customerId: string;
  readonly date: string;
}
